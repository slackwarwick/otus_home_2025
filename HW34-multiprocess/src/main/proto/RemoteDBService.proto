syntax = "proto3";

package ru.otus.protobuf;

option java_multiple_files = true;

message NumberRangeMessage {
    int32 start = 1;
    int32 end = 2;
}

message NumberMessage {
    int32 value = 1;
}

service RemoteNumberService {
    rpc getNumbers(NumberRangeMessage) returns (stream NumberMessage);
}
